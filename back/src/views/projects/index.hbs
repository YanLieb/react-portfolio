<ol>
  {{#each projects as |project|}}
    <li id="{{project._id}}">
      <ul>
        <li>
          {{project.title}}
        </li>
        <li>
          <a href="/project/{{project.slug}}">Open</a>
        </li>
      </ul>
    </li>
  {{/each}}
</ol>